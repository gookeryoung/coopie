# PySide2/PySide6 GUI 开发规范

适用于 `project_type=gui` 的生成项目。在 `rule-11-python-standards.md` 基础上补充 Qt 桌面应用的硬约束。完整设计令牌表、代码模板与参考示例见 `.trae/skills/gui-pyside2/SKILL.md`。

## 双兼容（关键约束）

- 模板按 Python 版本自动区分绑定：`PySide2`（≤3.10）/ `PySide6`（≥3.11），代码须双兼容。
- 导入一律 `try: PySide2 except ImportError: PySide6`；事件循环 `app.exec if hasattr(app, "exec") else app.exec_`。
- 枚举跨版本用短名（如 `Qt.AlignCenter`，两代均支持）；仅 PySide6 用全路径。
- 依赖声明由模板自动生成，勿手改：

```toml
dependencies = [
    "PySide2>=5.15.2.1; python_version <= '3.10'",
    "PySide6>=6.5.0; python_version >= '3.11'",
]
```

## 设计令牌

- 所有 QSS 与代码须引用 `src/{{ package_name }}/theme.py` 中的令牌常量，禁止散落硬编码颜色/尺寸/间距。
- QSS 用 `string.Template` 的 `${TOKEN}` 占位符，加载时 `substitute(theme.QSS_TOKENS)`；禁止内联硬编码样式。
- 8px 基准网格，所有间距须为 8 的倍数。
- 令牌完整清单见 SKILL.md「设计系统」。

## 布局

- 主窗口「头部 + 侧边栏 + 内容区 + 状态栏」四区结构，用 Layout 嵌套 + `QSplitter`，**禁用** `setGeometry`（固定尺寸弹窗除外）。
- 外层 `QVBoxLayout`：header → (sidebar + content 的 `QHBoxLayout`/`QSplitter`) → statusbar。
- Content 用 `QStackedWidget` 配合侧边栏 `currentRowChanged` 切换；侧边栏可折叠。
- 最小 `800×600`，默认 `1280×800`；宽 < 1000px 时侧边栏折叠为图标条（56px）。
- 所有 Layout `setContentsMargins` 用 `SPACING_*` 令牌。

## 禁止项

- 禁用 `setGeometry` 绝对定位（固定尺寸弹窗除外）。
- 禁用 `SIGNAL/SLOT` 字符串语法信号槽；用新式 `Signal()` + 方法引用连接。
- 禁止主线程执行长任务（冻结 UI）；用 `QThread` 或 `QThreadPool + QRunnable`。
- 禁止非主线程操作 GUI 部件；只通过信号回主线程。
- 禁止 `time.sleep` 阻塞事件循环；用 `QTimer`。
- 禁止裸 `except` 吞异常；worker 的 `run` 只捕获预期异常。

## 模块与入口

- 入口 `src/{{ package_name }}/main.py`：`QApplication(sys.argv)` → 构建主窗口 → 事件循环。
- `main()` 加 `# pragma: no cover`，拆出 `create_main_window()` 等可测函数。
- 业务逻辑放纯 Python 模块（不 import PySide）便于单测；GUI 层只做信号槽连接与状态展示。
- 重型部件惰性导入以加快启动。

## 线程与资源

- QThread 用 Worker 模式（QObject + `moveToThread`）；退出 `quit() + wait()` 或中断请求；`finished` → `deleteLater` 避免泄漏。
- 互斥锁用 `QMutex + QMutexLocker`（RAII）或 `threading.Lock`。
- `.qrc` 编译为 `_rc.py`（`pyside2-rcc`/`pyside6-rcc`）纳入版本控制；大文件用磁盘路径加载。

## 测试

- GUI 测试加 `@pytest.mark.gui`（或 `slow`），CI 默认 `-m "not slow"` 跳过。
- 无头环境设 `QT_QPA_PLATFORM=offscreen`。
- 详见 SKILL.md「测试（pytest-qt）」。
